export default defineNuxtConfig({
  compatibilityDate: "2024-11-01",
  future: { compatibilityVersion: 4 },
  srcDir: "app",

  modules: [
    "@nuxtjs/tailwindcss",
    "@pinia/nuxt",
    "@nuxt/icon",
  ],

  icon: {
    mode: "css",          // best for performance
    size: "24px",
    class: "icon",
  },

  devtools: { enabled: true },

  css: ["~/assets/css/main.css"],

  components: [
    {
      path: "~/components",
      pathPrefix: false,
    },
  ],

  dir: {
    pages: "pages",
    layouts: "layouts",
    middleware: "middleware",
  },

  runtimeConfig: {
    public: {
      apiBase: process.env.NUXT_PUBLIC_API_BASE || "http://localhost:8080",
      wsBase: process.env.NUXT_PUBLIC_WS_BASE || "ws://localhost:8080",
    },
  },

  app: {
    head: {
      title: "Platform",
      titleTemplate: "%s | Platform",
      meta: [
        { charset: "utf-8" },
        { name: "viewport", content: "width=device-width, initial-scale=1" },
      ],
    },
  },

  imports: {
    dirs: ["services"],
  },

  typescript: {
    strict: true,
  },

  tailwindcss: {
    viewer: false,
  },

  vite: {
    server: {
      hmr: {
        protocol: "ws",
        host: "localhost",
        port: 24678,
      },
    },
  },

  nitro: {
    experimental: {
      asyncContext: true,
    },
  },
});    

frontend/tailwind.config.ts
import type { Config } from "tailwindcss";

export default {
  content: ["./app/**/*.{vue,ts,js}"],

  theme: {
    extend: {
      fontFamily: {
        sans: ["Inter", "ui-sans-serif", "system-ui"],
        serif: ["Playfair Display", "ui-serif", "Georgia"],
      },

      colors: {
        brand: {
          50: "rgb(var(--brand-50) / <alpha-value>)",
          100: "rgb(var(--brand-100) / <alpha-value>)",
          200: "rgb(var(--brand-200) / <alpha-value>)",
          300: "rgb(var(--brand-300) / <alpha-value>)",
          400: "rgb(var(--brand-400) / <alpha-value>)",
          500: "rgb(var(--brand-500) / <alpha-value>)",
          600: "rgb(var(--brand-600) / <alpha-value>)",
          700: "rgb(var(--brand-700) / <alpha-value>)",
          800: "rgb(var(--brand-800) / <alpha-value>)",
          900: "rgb(var(--brand-900) / <alpha-value>)",
        },

        surface: {
          50: "rgb(var(--surface-50) / <alpha-value>)",
          100: "rgb(var(--surface-100) / <alpha-value>)",
          200: "rgb(var(--surface-200) / <alpha-value>)",
          300: "rgb(var(--surface-300) / <alpha-value>)",
        },

        accent: "rgb(var(--accent) / <alpha-value>)",
        muted: "rgb(var(--muted) / <alpha-value>)",
      },
    },
  },

  plugins: [],
} satisfies Config;


frontend/app/assets/css/main.css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ===============================
   THEME — Hotel (Ivory inspired)
   =============================== */

:root {
  /* Brand (warm ivory / stone / olive tones) */
  --brand-50: 250 248 245;
  --brand-100: 242 237 230;
  --brand-200: 228 217 204;
  --brand-300: 210 191 171;
  --brand-400: 189 160 136;
  --brand-500: 155 151 146;
  --brand-600: 121 117 110;
  --brand-700: 73 74 46;
  --brand-800: 67 74 13;
  --brand-900: 35 39 51;

  /* Surfaces */
  --surface-50: 255 255 255;
  --surface-100: 242 242 242;
  --surface-200: 211 212 217;
  --surface-300: 73 74 80;

  /* Accent (subtle hotel blue) */
  --accent: 128 169 229;

  /* Muted text */
  --muted: 155 151 146;
}

/* ===============================
   BASE STYLES
   =============================== */

body {
  @apply bg-surface-50 text-brand-900 font-sans;
}

.btn-primary {
  @apply bg-brand-700 text-white hover:bg-brand-800 transition px-4 py-2 rounded-lg;
}

.card {
  @apply bg-white border border-surface-200 rounded-xl shadow-sm;
}