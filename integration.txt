Good — you’re at the stage where **structure matters more than features**. If you don’t slice this properly, Sonnet will generate inconsistent API usage, duplicated logic, and broken page integrations.

Below is a **clean integration slicing system** specifically for your monorepo.

---

# 🧱 MASTER FRONTEND ↔ BACKEND INTEGRATION SLICES

Each slice is:

* 🔧 A **self-contained integration unit**
* 📦 Has **exact backend files + frontend files**
* 🚫 No mock data allowed inside that slice
* ✅ Must end in a working UI connected to API

---

# 🟦 SLICE 0 — API FOUNDATION (DO THIS FIRST)

## 🎯 Goal

Frontend can reliably talk to backend (no UI logic yet).

---

## Backend files (must exist & be stable)

* `pkg/response/response.go`
* `pkg/apierror/apierror.go`
* `middleware/cors.go`
* `middleware/auth.go`
* `middleware/logger.go`
* `config/config.go`
* `database/database.go`

---

## Frontend files

* `app/composables/useApi.ts`
* `app/plugins/api.client.ts`
* `app/utils/http-errors.ts`
* `app/types/api.types.ts`

---

## Must implement

* Base API wrapper (`useApi`)
* Error normalization (401/500 handling)
* Base URL injection from `.env`
* Request/response typing

---

## Output of this slice

✔ Frontend can call:

```
GET /api/v1/health
GET /api/v1/items
```

without breaking

---

# 🟩 SLICE 1 — AUTH SYSTEM INTEGRATION

## 🎯 Goal

Login/register + session persistence works end-to-end.

---

## Backend files

* `internal/auth/*`
* `internal/users/*`
* `internal/roles/*`
* `middleware/auth.go`
* `middleware/rbac.go`

---

## Frontend files

* `services/auth.service.ts`
* `composables/useAuth.ts`
* `stores/auth.store.ts`
* `plugins/auth.client.ts`

### Pages

* `pages/auth/login.vue`
* `pages/auth/register.vue`

---

## Must implement

* Login API → store JWT
* Auto attach token to requests
* Refresh token flow (if exists)
* Protect routes (`auth.global.ts`)
* Redirect logic (guest/auth middleware)

---

## Output

✔ User can login
✔ Token persists
✔ Protected pages block unauth users

---

# 🟨 SLICE 2 — ITEMS (ROOMS SYSTEM CORE)

## 🎯 Goal

Replace ALL mock room data with real backend data.

---

## Backend files

* `internal/items/domain.go`
* `internal/items/service.go`
* `internal/items/handler.go`
* `internal/items/routes.go`
* `database/migrations/000004_create_items.up.sql`

---

## Frontend files

* `services/items.service.ts` (already exists)
* `composables/useItems.ts`
* `pages/index.vue` ❗ (important)
* `pages/items/index.vue`
* `pages/items/[id].vue`
* `components/items/ItemGrid.vue`
* `components/items/ItemCard.vue`

---

## Must implement

### API usage

* `GET /api/v1/items`
* `GET /api/v1/items/:id`

---

### Replace in index page

❌ REMOVE:

```ts
const featuredRooms = [...]
```

✔ REPLACE WITH:

```ts
const { data: items } = await itemsService.list()
```

---

## Output

✔ Home page shows real rooms
✔ Rooms page fully dynamic
✔ Detail page works

---

# 🟧 SLICE 3 — ORDERS (BOOKING FLOW)

## 🎯 Goal

User can book a room (end-to-end flow)

---

## Backend files

* `internal/orders/*`
* `database/migrations/000005_create_orders.up.sql`

---

## Frontend files

* `services/orders.service.ts`
* `composables/useOrders.ts`
* `stores/cart.store.ts`
* `pages/orders/index.vue`
* `pages/orders/[id].vue`
* `components/orders/*`

---

## Must implement

* Create order from item
* Fetch user orders
* Order status tracking

---

## Flow

```
Item → Order → Confirmation → Status page
```

---

## Output

✔ Booking system works
✔ Orders persist in DB
✔ User sees history

---

# 🟥 SLICE 4 — PAYMENTS (REAL TRANSACTIONS)

## 🎯 Goal

Attach payment to orders

---

## Backend files

* `internal/payments/*`
* `internal/payments/providers/*`
* `events/handlers/process_payment.go`

---

## Frontend files

* `services/payments.service.ts`
* `composables/usePayments.ts`
* `components/payments/*`

---

## Must implement

* Initiate payment
* Payment status polling OR websocket update
* Link payment → order

---

## Output

✔ Payment flow completes order lifecycle

---

# 🟪 SLICE 5 — NOTIFICATIONS + WEBSOCKETS

## 🎯 Goal

Real-time updates (critical UX upgrade)

---

## Backend files

* `internal/notifications/*`
* `internal/websocket/*`
* `events/handlers/*`

---

## Frontend files

* `plugins/websocket.client.ts`
* `composables/useWebSocket.ts`
* `stores/notification.store.ts`
* `components/notifications/*`

---

## Must implement

* WebSocket connection on login
* Order status updates
* Notification push UI

---

## Output

✔ Live updates without refresh

---

# 🟫 SLICE 6 — SEARCH SYSTEM

## 🎯 Goal

Homepage + items search works dynamically

---

## Backend files

* `internal/search/*`

---

## Frontend files

* `services/search.service.ts`
* `composables/useSearch.ts`
* `components/shared/SearchInput.vue`
* `components/items/ItemSearchBar.vue`

---

## Must implement

* Query items by keyword
* Debounced search
* Live results update

---

## Output

✔ Search bar becomes functional system-wide

---

# 🟩 SLICE 7 — UPLOADS + GALLERY

## 🎯 Goal

Rooms/images can be uploaded + displayed

---

## Backend files

* `internal/uploads/*`

---

## Frontend files

* `services/upload.service.ts`
* `components/shared/FileUploader.vue`
* `pages/gallery.vue` ❗ (create)

---

## Must implement

* Upload images
* Attach to items
* Render gallery grid

---

## Output

✔ Real media system (no placeholders)

---

# 🟦 SLICE 8 — ADMIN PANEL INTEGRATION

## 🎯 Goal

Full backend control UI

---

## Backend files

* `internal/admin/*`

---

## Frontend files

* `services/admin.service.ts`
* `pages/admin/*`
* `components/admin/*`

---

## Must implement

* Users list
* Items management
* Orders overview
* Stats dashboard

---

## Output

✔ Fully functional admin system

---

# 🟥 SLICE 9 — STATIC PAGES + HYBRID DATA

## 🎯 Goal

Finish remaining pages using backend where needed

---

## Pages

* `pages/about.vue` (stats from backend)
* `pages/contact.vue` (POST message endpoint)
* `pages/restaurant.vue` (filtered items)
* `pages/gallery.vue`

---

## Backend additions

* `POST /api/v1/contact`
* optional: `GET /api/v1/stats`

---

## Output

✔ Marketing pages connected to backend

---

# ⚠️ CRITICAL RULE FOR SONNET GENERATION

For EVERY slice, enforce:

### ❌ NEVER:

* mock arrays
* static featuredRooms
* fake API responses

### ✅ ALWAYS:

* use `services/*`
* use `composables/*`
* use `useApi()`
* fetch from backend only

---

# 🚀 FINAL EXECUTION ORDER

Run in this exact order:

```
Slice 0 → Slice 1 → Slice 2 → Slice 3 → Slice 4 → Slice 5 → Slice 6 → Slice 7 → Slice 8 → Slice 9
```

---

# If you want next step

I can generate:

### 🧠 “Claude Sonnet Master Prompt (per slice automation)”

which will:

* auto-generate code per slice
* enforce API correctness
* prevent mock data leakage
* ensure full integration consistency

Just say:

> “generate sonnet slice prompt system”

and I’ll build it for you.
